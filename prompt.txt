rf 수신 데이터중에 {SA}에 대한 응답 처리부분 알려줘

if not (48 <= frame[1] <= 57 and 48 <= frame[2] <= 57):
    return False

이부분에서 
frame[1]이 '1' 인 경우 현재 동작 모드 그대로 유지하고

frame[1]이 '0' 인 경우 수동 동작 모드 전환및 모터 ON

frame[1]이 '1' 인 경우 이전의 동작 모드로 복귀

되도록 처리해줘
